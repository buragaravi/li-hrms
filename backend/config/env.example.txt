# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration (Main Database)
MONGODB_URI=mongodb://localhost:27017/hrms

# MSSQL Configuration (Biometric & Employee Data)
SQL_SERVER=localhost
SQL_DATABASE=master          # change to the biometric database when ready
SQL_PORT=1434
SQL_AUTH=sql
SQL_DOMAIN=PYDAHSOFT-2
SQL_USER=ravi
SQL_PASSWORD=ravi
SQL_ENCRYPT=true             # SSMS shows "Encrypt: Mandatory"
SQL_TRUST_SERVER_CERTIFICATE=true

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Super Admin Seed
SUPER_ADMIN_EMAIL=admin@hrms.com
SUPER_ADMIN_PASSWORD=Admin@123

